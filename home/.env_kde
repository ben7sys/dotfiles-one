# KDE-specific environment variables

# Set KDE_SESSION_VERSION to 5 for KDE Plasma 5
#export KDE_SESSION_VERSION=5

# Configure default KDE applications and set full session flag
#export KDEHOME="$HOME/.kde"  # Directory for KDE configuration files
#export KDE_FULL_SESSION=true  # Indicates a full KDE session is running

# Add Flatpak exports to PATH for KDE
#export PATH="$PATH:$HOME/.local/share/flatpak/exports/bin"

# Use native KDE file dialogs in GTK applications
#export GTK_USE_PORTAL=1

# Set default sound card for ALSA in KDE
#export ALSA_CARD=PCH  # Change this to match your sound card if needed

# Configure KWin compositing (OpenGL ES 2.0)
#export KWIN_COMPOSE=O2ES

# Set KDE as the current desktop environment
#export XDG_CURRENT_DESKTOP=KDE
#export XDG_SESSION_DESKTOP=KDE

# Use KDE's platform theme for Qt applications
#export QT_QPA_PLATFORMTHEME=kde

# Enable automatic HiDPI scaling for Qt applications
#export QT_AUTO_SCREEN_SCALE_FACTOR=1

# Disable MySQL driver for Baloo file indexer to improve performance
#export BALOO_DISABLE_MYSQL_DRIVER=1

# Additional KDE-specific configurations can be added here
# For example:
# export KDE_DEBUG=1  # Enable debug output for KDE applications
# export PLASMA_USE_QT_SCALING=1  # Use Qt scaling instead of KDE's own scaling